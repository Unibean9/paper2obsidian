import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const Color primary = Color(0xFF111111);
  static const Color background = Color(0xFFF4F0E8);
  static const Color backgroundDark = Color(0xFF111111);

  static const Color textPrimary = Color(0xFF111111);
  static const Color textSecondary = Color(0xFF5C5C5C);
  static const Color textMuted = Color(0xFF666666);
  static const Color textInverse = Color(0xFFF4F0E8);

  static const Color accent = Color(0xFF5B0000);

  static const Color surfaceLight = Color(0xFFFDFCF9);
  static const Color surfaceNeutral = Color(0xFFECE8E1);
  static const Color border = Color(0xFFD8D2C8);

  static const Color primaryHover = Color(0xFF333333);
  static const Color secondaryHoverBackground = Color(0xFFECE8E1);
  static const Color secondaryActiveBackground = Color(0xFFD8D2C8);

  static const Color linkHover = Color(0xFF5B0000);
  static const Color linkVisited = Color(0xFF666666);

  static const Color focusRing = Color(0x33111111);

  static const Color success = Color(0xFF2F6B2F); // fallback
  static const Color successSurface = Color(0xFFEAF3EA); // fallback
  static const Color warning = Color(0xFF9A5A00); // fallback
  static const Color warningSurface = Color(0xFFF6E8D1); // fallback
  static const Color error = Color(0xFF8A1F1F); // fallback
  static const Color errorSurface = Color(0xFFF7E5E5); // fallback
  static const Color infoSurface = Color(0xFFECE8E1); // fallback
}
